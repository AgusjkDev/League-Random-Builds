/// <reference path="./build.d.ts" />

type GetChamps = {
    champs: Champ[];
};

type GetRandomBuild = {
    randomBuild: RandomBuild;
};

type AllItems = {
    boots: Item[];
    legendaries: Item[];
    mythics: Item[];
};
